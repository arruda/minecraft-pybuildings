============
Installation
============

At the command line::

    $ easy_install minecraft-pybuildings

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv minecraft-pybuildings
    $ pip install minecraft-pybuildings